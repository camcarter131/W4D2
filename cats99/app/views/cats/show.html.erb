<h2>Cat!</h2>

<table>
    <thead>
        <tr>
            <th colspan="6">Cat!</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><%= @cat.id %></td>            
            <td><%= @cat.name %></td>
            <td><%= @cat.birth_date %></td>
            <td><%= @cat.color %></td>
            <td><%= @cat.sex %></td>
            <td><%= @cat.description %></td>
        </tr>
    </tbody>
</table>